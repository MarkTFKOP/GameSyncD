# Copy to env.sh and fill values.
# This file is safe to commit.

export STEAM_API_KEY=""
export STEAM_ID=""

# Raw Cookie header string from a logged-in GOG request.
export GOG_COOKIE=""

# Output folder for generated JSON files.
export OUTPUT_DIR="data"
